import { WriteScreen } from "@/components/testament/WriteScreen";

/** The ritual. The content is a client leaf because it reads the wallet and the language. */
export default function WritePage() {
  return <WriteScreen />;
}
