import { SceneHero } from "@/components/scene/SceneHero";

/** The scene. The content is a client leaf because it reads the chain and the language. */
export default function ScenePage() {
  return <SceneHero />;
}
